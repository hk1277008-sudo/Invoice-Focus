Act as a senior SaaS product designer, print/PDF UX specialist, frontend architect, and production UI engineer.

IMPORTANT:
This is a FINAL production-quality refinement of the InvoiceFocus document renderer.

DO NOT create a superficial CSS makeover.
DO NOT rewrite unrelated application code.
DO NOT change business logic.
DO NOT change the invoice calculation engine.
DO NOT change database schemas.
DO NOT change API behavior.
DO NOT change authentication.
DO NOT change dashboard functionality.
DO NOT change document creation/editing workflows.
DO NOT change existing data models.
DO NOT change PDF generation architecture.
DO NOT add unnecessary dependencies.

Your job is ONLY to audit and improve the existing document rendering / printable document layer.

==================================================
CURRENT PROBLEM
==================================================

The current generated PDF/print output still does not look like a premium SaaS business document.

The attached screenshot shows the current result.

Critical visible bug:
"Grand Total" is currently rendered TWICE.

Example:

Grand Total     $5,000.00

and then another:

Grand Total     $5,000.00

This must be fixed at the renderer/source level.

Do NOT simply hide one duplicate with CSS.

Find why the total is being rendered twice and correct the underlying rendering structure so each document has exactly one authoritative totals section.

==================================================
PRODUCT STANDARD
==================================================

InvoiceFocus should visually compete with the quality level users expect from modern professional SaaS products such as:

- Stripe
- Adobe
- Zoho
- Notion
- Replit
- Linear
- Vercel
- QuickBooks
- Xero

Do NOT copy any company's design literally.

Use their general design principles:

- strong hierarchy
- excellent spacing
- restrained typography
- clear information grouping
- professional tables
- excellent print readability
- consistent alignment
- intentional whitespace
- strong but restrained branding
- predictable scanning patterns
- premium visual polish

The result must feel like a serious business document generated by a professional invoicing platform.

==================================================
DOCUMENT TYPES
==================================================

The renderer supports SIX document categories:

1. Invoice
2. Receipt
3. Estimate
4. Quote
5. Credit Note
6. Purchase Order

There are THREE visual families:

1. Minimal
2. Professional
3. Enterprise

That means the renderer must correctly support:

6 document types × 3 visual families = 18 combinations.

Audit and improve ALL 18 combinations.

Do not optimize only Invoice.

Every document type must feel intentionally designed for its purpose.

==================================================
VISUAL DIRECTION
==================================================

Use this structural direction as the design foundation:

┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  [LOGO]                                                      │
│  Business Name                              DOCUMENT TYPE     │
│  Contact information                        DOCUMENT NUMBER   │
│  Address                                    Issue Date        │
│  email · phone                              Due Date          │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  BILL TO / CUSTOMER                      PAYMENT / DETAILS   │
│  Customer name                             Payment terms      │
│  Company                                   PO / reference     │
│  Address / contact                         Status             │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  DESCRIPTION                 QTY       PRICE        AMOUNT   │
│  ──────────────────────────────────────────────────────────   │
│  Item                        10       $500.00      $5,000.00 │
│                                                              │
│                                      Subtotal     $5,000.00   │
│                                      Tax               —      │
│                                      Discount          —      │
│                                      ─────────────────────   │
│                                      TOTAL        $5,000.00  │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  PAYMENT DETAILS                                             │
│  Bank / payment information                                  │
│                                                              │
│  NOTES                                                        │
│  Thank you for your business.                                │
│                                                              │
│  TERMS & CONDITIONS                         InvoiceFocus     │
└──────────────────────────────────────────────────────────────┘

This is a STRUCTURAL reference only.

Improve it substantially where appropriate.

Do not literally reproduce the ASCII sketch.

==================================================
CRITICAL LAYOUT REQUIREMENTS
==================================================

1. A4-first design

The printable document must be designed specifically for:

- A4 portrait
- professional printer margins
- browser print
- Microsoft Print to PDF
- Chrome/Edge PDF
- multi-page documents

Do not design the PDF as if it were a web page screenshot.

2. Remove excessive whitespace

The current document has too much empty vertical space.

Use whitespace intentionally, but compact the document enough that normal invoices look complete and professional on one page.

Do not artificially stretch sections.

3. Strong header

The header should clearly establish:

LEFT:
- logo
- business name
- optional business/contact information

RIGHT:
- document type
- document number
- issue date
- due date when applicable
- status when applicable

The document title should be visually dominant without becoming oversized.

4. Logo

Use the existing uploaded/business logo data.

Do not distort it.

Maintain aspect ratio.

Prevent extremely small logos where a reasonable larger presentation is possible.

If no logo exists, the layout must still look professional.

5. Typography

Use a professional SaaS-style typography system.

Recommended characteristics:

- clean sans-serif
- strong heading hierarchy
- restrained font weights
- readable body text
- small uppercase metadata labels
- consistent number alignment

Avoid:
- excessive bold text
- giant headings
- decorative typography
- unnecessary colors

6. Color

Use InvoiceFocus branding intelligently.

Primary brand color:
#2563EB

Do not make the whole document blue.

Use brand color primarily for:
- document accent
- important totals
- subtle rules
- status indicators
- small UI-like highlights

The document should remain mostly white / neutral / professional.

7. Tables

Tables must look premium.

Requirements:

- clear column alignment
- numeric values right aligned
- quantity centered/right aligned appropriately
- description left aligned
- subtle row separators
- strong header distinction
- no excessive borders
- long descriptions must wrap correctly
- multiple items must remain readable
- table headers must repeat on subsequent pages

8. Totals

There must be exactly ONE totals block.

Never render "Grand Total" twice.

The totals structure should support:

- subtotal
- tax
- discount
- shipping/fees if existing data supports them
- paid amount if applicable
- balance due if applicable
- grand total / total due according to document type

Only show fields that are actually applicable.

Do not invent financial values.

Use the existing calculation/data model.

The final total must be visually prominent but not oversized.

9. No duplicated content

Audit the complete renderer for:

- duplicated Grand Total
- duplicated subtotal
- duplicated document number
- duplicated dates
- duplicated customer information
- duplicated footer content
- duplicated status
- duplicated payment information

If duplication is caused by multiple components rendering the same data, fix the component architecture.

Do not hide duplicated elements with CSS.

==================================================
DOCUMENT-SPECIFIC BEHAVIOR
==================================================

Do not simply replace the word "Invoice".

Each document type should have appropriate semantics.

INVOICE
- Invoice
- Invoice number
- Issue date
- Due date
- Status
- Bill To
- Payment terms
- Items
- Subtotal
- Tax/discount where applicable
- Total
- Payment details
- Notes
- Terms

RECEIPT
- Receipt
- Receipt number
- Date
- Customer
- Payment status
- Payment method where existing data supports it
- Purchased items
- Amount paid
- Total
- Appropriate receipt confirmation language

Do NOT show invoice-only fields when they make no sense.

ESTIMATE
- Estimate
- Estimate number
- Issue date
- Valid until where existing data supports it
- Customer
- Items
- Estimated subtotal
- Tax/discount where applicable
- Estimated total
- Appropriate estimate terminology

QUOTE
- Quote
- Quote number
- Issue date
- Valid until where existing data supports it
- Customer
- Items
- Quote total
- Appropriate quotation terminology

CREDIT NOTE
- Credit Note
- Credit note number
- Issue date
- Customer
- Reference to original invoice where existing data supports it
- Credited items
- Credit amount
- Appropriate credit terminology

PURCHASE ORDER
- Purchase Order
- PO number
- Issue date
- Delivery/due information where existing data supports it
- Vendor/supplier information
- Items
- Subtotal
- Tax/other applicable amounts
- Total
- Appropriate purchase-order terminology

Do not fabricate data that is not present.

==================================================
THREE VISUAL FAMILIES
==================================================

MINIMAL

Goal:
Elegant, extremely clean, modern, restrained.

Characteristics:
- generous but controlled whitespace
- thin rules
- minimal decoration
- neutral palette
- subtle InvoiceFocus blue accents
- excellent typography
- compact metadata
- sophisticated table

It should feel like a premium modern startup document.

PROFESSIONAL

Goal:
Professional business accounting document.

Characteristics:
- stronger section separation
- clearer metadata blocks
- stronger table hierarchy
- more obvious totals
- moderate brand accent
- excellent scanning
- balanced information density

It should feel suitable for freelancers, agencies, consultants and small businesses.

ENTERPRISE

Goal:
High-end corporate document.

Characteristics:
- strongest information hierarchy
- structured metadata
- sophisticated header
- stronger section architecture
- refined borders/rules
- premium totals treatment
- excellent multi-page behavior
- corporate-level visual consistency

It should feel suitable for larger organizations and finance departments.

IMPORTANT:

The three families must be genuinely different in visual hierarchy and composition.

Do NOT merely change:
- border radius
- color
- font size

They should feel like three distinct professional document systems.

==================================================
FOOTERS
==================================================

Preserve support for the existing:

- Simple footer
- Detailed footer
- Bar footer

But redesign them to match the new premium document system.

Footer must never collide with totals.

Footer must never duplicate totals.

Footer must never overlap content.

Footer should remain consistent across multi-page output.

==================================================
MULTI-PAGE BEHAVIOR
==================================================

Normal documents should fit on one page whenever realistically possible.

Long documents must paginate naturally.

Requirements:

- repeat table header on new pages
- avoid splitting rows where possible
- keep totals together
- avoid orphaned headings
- avoid footer overlap
- preserve consistent page margins
- preserve document identity on subsequent pages
- no clipped content
- no duplicate totals caused by pagination
- no duplicate footer caused by pagination

==================================================
PRINT / PDF
==================================================

The production PDF mechanism must remain the current browser-print / printable HTML path.

Do not introduce Playwright into the runtime.

Do not add Chromium as an application dependency.

Do not replace the current PDF architecture.

Improve the printable HTML/CSS itself.

Use @page and print-specific CSS correctly.

Ensure:

- A4 page size
- predictable margins
- print-color-adjust where appropriate
- backgrounds/colors remain intentional
- no web-only controls appear in print
- no browser UI elements become part of the document
- no horizontal overflow
- no unexpected blank pages

==================================================
DATA AND CALCULATION SAFETY
==================================================

ABSOLUTE REQUIREMENT:

The existing data and calculation behavior must remain 100% unchanged.

Do not modify:

- subtotal calculations
- tax calculations
- discount calculations
- total calculations
- currency conversion
- currency formatting logic
- quantity calculations
- payment calculations
- document status logic
- invoice numbering
- document numbering
- database data
- API responses
- authentication
- persistence
- dashboard
- invoice editor
- document creation

The renderer should consume the existing data model.

Only presentation/rendering behavior may change.

==================================================
CURRENCY SAFETY
==================================================

Preserve the existing currency behavior exactly.

Pay special attention to:

- JPY zero-decimal formatting
- CNY zero-decimal behavior where already supported
- AED formatting
- SAR formatting
- currency prefixes/suffixes
- decimal precision

Do not rewrite currency calculations.

Ensure preview and printable output remain identical.

==================================================
LIVE PREVIEW PARITY
==================================================

The live document preview and printable/PDF output must use the same pure rendering model wherever the existing architecture supports it.

What the user sees in preview must match what gets printed.

No:

"Preview looks good but PDF looks different."

==================================================
QUALITY BAR
==================================================

Before finishing, inspect actual generated output.

Do not assume the CSS is correct.

Generate real PDFs/print output for representative examples.

At minimum inspect:

1. Invoice Minimal
2. Invoice Professional
3. Invoice Enterprise
4. Receipt
5. Estimate
6. Quote
7. Credit Note
8. Purchase Order

Also inspect:

- multiple line items
- long descriptions
- long customer names
- missing optional fields
- tax
- discount
- partial payment/status
- different currencies
- long multi-page document

==================================================
SPECIFIC BUG FROM CURRENT SCREENSHOT
==================================================

The screenshot currently shows:

Grand Total $5,000.00

followed by another:

Grand Total $5,000.00

Fix this first.

The final output must contain ONE and ONLY ONE authoritative total.

Also correct the excessive whitespace visible between:

- header
- customer section
- item table
- totals
- footer

The final page should feel intentionally composed rather than vertically stretched.

==================================================
IMPLEMENTATION PROCESS
==================================================

STEP 1
Audit the existing renderer architecture.

Identify:

- renderer entry points
- document model
- template/family selection
- invoice/receipt/estimate/quote/credit-note/purchase-order rendering
- totals component
- footer component
- print CSS
- pagination CSS
- preview rendering
- PDF/print rendering

STEP 2
Find the exact cause of duplicate Grand Total.

Fix the source.

STEP 3
Implement the improved shared rendering structure.

STEP 4
Implement/refine the three visual families:

- Minimal
- Professional
- Enterprise

STEP 5
Apply the visual families across all six document types.

STEP 6
Fix print/A4 pagination.

STEP 7
Generate actual PDFs and inspect them.

STEP 8
Fix any visual problems discovered during inspection.

STEP 9
Run all project verification.

==================================================
VERIFICATION
==================================================

Run:

- TypeScript check
- production build
- git diff --check
- existing renderer tests
- renderer matrix tests if available

Verify all:

6 document types × 3 visual families = 18 combinations.

Verify:

- no duplicate totals
- no duplicate content
- no clipped content
- no overlapping content
- no unexpected blank pages
- no broken currency formatting
- no broken calculations
- no broken statuses
- no broken print flow
- no broken preview
- no broken long-document pagination

==================================================
STRICT FILE / FUNCTIONALITY SAFETY
==================================================

Do not touch unrelated application functionality.

If a change appears necessary outside the renderer, STOP and explain why instead of modifying unrelated functionality.

Do not change:

- auth
- dashboard
- API
- database
- Supabase
- invoice creation logic
- invoice calculations
- document persistence
- document sharing
- PDF download mechanism
- routes
- SEO
- deployment
- environment variables

unless a renderer-only change absolutely requires it.

==================================================
FINAL REPORT
==================================================

After implementation, report:

1. Root cause of duplicate Grand Total
2. Exact renderer files changed
3. What changed visually
4. How Minimal differs from Professional
5. How Professional differs from Enterprise
6. How all six document types were handled
7. Print/PDF improvements
8. Pagination improvements
9. Verification results
10. Confirmation that calculations/data/API/auth/database/dashboard functionality were not modified

Do not claim success unless you actually verify the generated output.

The final result must look like a premium SaaS-generated business document, not a webpage printed onto paper.